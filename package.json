{
  "name": "experience-intelligence-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch server.mjs",
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check scripts/smoke.mjs",
    "test": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
